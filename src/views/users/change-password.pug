extends ../base.pug 

block content
    if errorMessage
        span=errorMessage
    form(method="post")
        input(name="old", type="password",placeholder="Old Password")
        input(name="change",type="password",placeholder="New Password")
        input(name="confirm",type="password",placeholder="New Password Confirmation")
        input(type="submit", value="Change Password")